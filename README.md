#   ѕрограмма дл€ фильтрации строк по длине

*   Ёта программа принимает массив строк в качестве входных данных и генерирует новый массив строк, 
длина которых меньше или равна заданному пользователем значению (в задании оно равно 3).
»сходный массив строк должен быть введен пользователем 
ѕрограмма не использует коллекции, а использует только массивы. *

##  Ѕлок-схема программы приведена ниже:

flowchart TD
    A([Start])
    subgraph vvod
    B[/"quantityStr\n LengthString\n arrStrings[quantityStr]\n"/]
    ==>C["arrNumberStrLen3[quantityStr]\n numberStrLen3 = 0\n"]
    end
    D{{numStr = 0 ... quantityStr}}
    D==True==>E{"Length(arrStrings[numStr]) <= LengthString"}
    E==Yes==>G["arrNumberStrLen3[numberStrLen3] = numStr\n numberStrLen3 = numberStrLen3 + 1"]==>F
    E==No==>F["numStr = numStr + 1"]==>D
    D==False==>H["arrStrLen3[numberStrLen3]\n"]==>J{{numStr = 0 ... numberStrLen3}}
    J==True==>K["arrStrLen3[numStr] = arrStrings[arrNumberStrLen3[numStr]]\n numStr = numStr + 1"]==>J
    J==False==>M[/"display_out(arrStrLen3)"/]==>N([End])
    A==>vvod==>D

##  ѕрограмма работает следующим образом:

### 1. ѕользователь вводит количество строк и длину строк дл€ их последующей фильтрации
### 2. Ќа основе этой информации создаетс€ массив строк, в который пользователь вводит исходные данные.
### 3. «атем программа сравнивает длину каждой строки во входном массиве с заданной.
### 4. ≈сли длина меньше либо равна заданной длине, то программа сохран€ет индекс этой строки в новом массиве.
### 5. ƒалее программа использует полученный массив индексов дл€ извлечени€ строк из исходного массива, длина которых меньше либо равна требуемой длине и заносит их в созданный дл€ этого финальный массив.
### 6. Ќаконец, финальный массив строк выводитс€ на экран пользовател€.
